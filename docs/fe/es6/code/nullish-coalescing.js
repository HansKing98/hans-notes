/* ?? 运算符 */
const a = 0 ?? 'hansking'         // 0
const b = '' ?? 'hansking'        // ''
const c = null ?? 'hansking'      // 'hansking'
const d = undefined ?? 'hansking' // 'hansking'

/* || 运算符 */
const A = 0 || 'hansking'         // 'hansking'
const B = '' || 'hansking'        // 'hansking'
const C = null || 'hansking'      // 'hansking'
const D = undefined || 'hansking' // 'hansking'
